# Local-dev defaults, safe to commit (no secrets). Loaded before the
# optional Grafana Cloud override file, so that file's OTEL_* values (if
# present) take precedence -- env_file entries later in the list win.
OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318/v1/traces
